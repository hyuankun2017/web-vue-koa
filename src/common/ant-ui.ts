/**
 * 按需引入design-ant组件，使用次数较多的
 */
import Vue from 'vue'
import { Button, Input, Select, Checkbox, Icon } from 'ant-design-vue'

Vue.use(Button)
Vue.use(Input)
Vue.use(Select)
Vue.use(Checkbox)
Vue.use(Icon)

